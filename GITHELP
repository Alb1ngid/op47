1 раз
git init - инициализация в гите
git remote add origin link (ваша ссылка)- привязывает проект и ваш репозитоирий

регулярные команды
git add - добавление в репозиторий
git commit -m 'name' - точка сохранения
git push origin namebranch - загружает проект в репозиторий
.gitignore/
git rm --cached namefile (-r)
git clone link(ссылка на репозиторий)-клонирует\скачивает репозиторий


python3/py3/py/python -m venv namefile

namefile/Scripts/activate
source namefile/bin/activate

pip freeze
pip list
pip freeze > requairements.txt
pip install name

git branch
git branch name
git checkout name

